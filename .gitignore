# dependencies
node_modules
.pnp
.pnp.*

# bun.lock is the single lockfile (vercel.json pins `bun install`)
package-lock.json
yarn.lock

# testing
coverage

# next.js
.next/
out/
build
dist

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dev.log
*.log

# env files — NEVER commit real secrets
.env
.env.local
.env.*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma sqlite (local only)
db/*.db

# claude code local state
.claude/

# agent instruction files — local only, README is the sole repo doc
AGENTS.md
CLAUDE.md
