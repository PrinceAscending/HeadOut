import { WorldExperience } from "@/components/world/WorldExperience";

export default function Page() {
  return (
    <main className="w-screen h-screen overflow-hidden" aria-label="PRINCE // WORLD — explorable digital universe">
      <WorldExperience />
    </main>
  );
}
